name: 'dbt_project'
version: '1.0.0'
profile: 'dbt_project'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt_project:
    bronze_layer:
      bronze_football:
        +schema: brz_football
    silver_layer:
      silver_football:
        +schema: slv_football
    gold_layer:
      gold_football:
        +schema: gld_football